{% extends '::base.html.twig' %}
{% form_theme edit_form 'DevTaskBundle:form:fields.html.twig' %}

{% block body -%}
    <div class="wrap">
    <h1>Task edit</h1>

    {{ form(edit_form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('task') }}">
                Back to the list
            </a>
        </li>
        {#<li>{{ form(delete_form) }}</li>#}
    </ul>
    </div>
{% endblock %}
